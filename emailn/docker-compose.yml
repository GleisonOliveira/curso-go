services:
  postgres:
    image: postgres:16-alpine
    container_name: emailn-postgres
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: emailn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
